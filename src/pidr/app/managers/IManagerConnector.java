package pidr.app.managers;

/*This class may come in use if one day it is decided not to use matatlab but 
 * another program, and in the rest of the code replace calls to 
 * managerMatlab.getInstance() with calls to managerConnector.getInstance() 
 * (which will be the same thing if the current connector is matlab).
 */
public interface IManagerConnector {
	//placeholder
}
